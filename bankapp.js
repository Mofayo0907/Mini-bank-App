const login = () => {
let newCustomer ={
    aNumber : accountnumber.value,
    pword : password.value
}

    if ((aNumber = accountnumber.value) && (pword = password.value))

    aNumber.value = ""
    password.value = ""


console.log(newCustomer)
}